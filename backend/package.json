{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "scripts": {
    "dev": "bun src/index.ts",
    "build": "bun build --compile ./src/index.ts --outfile=app",
    "test": "bun test"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "jest": "^29.7.0"
  }
}